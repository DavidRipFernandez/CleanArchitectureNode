{
  "name": "test-sps-server",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "start": "node src/index.js",
    "dev": "start /B cmd /c \"set NODE_ENV=dev && set PORT=3001 && pm2 start src/index.js --name backend-dev --watch\"",
    "test": "start /B cmd /c \"set NODE_ENV=test && set PORT=3002 && pm2 start src/index.js --name backend-test --watch\"",
    "staging": "start /B cmd /c \"set NODE_ENV=staging && set PORT=3003 && pm2 start src/index.js --name backend-staging --watch\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mssql": "^11.0.1",
    "swagger-jsdoc": "^6.2.8",
    "swagger-ui-express": "^5.0.1",
    "yamljs": "^0.3.0"
  },
  "devDependencies": {
    "jest": "^30.0.5",
    "nodemon": "^3.1.0",
    "supertest": "^7.1.4"
  }
}
