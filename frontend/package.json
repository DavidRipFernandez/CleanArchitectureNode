{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "axios": "^1.11.0",
    "bootstrap": "^5.3.7",
    "cra-template-bootstrap": "0.0.0",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.8.0",
    "react-scripts": "5.0.1",
    "sweetalert2": "^11.22.4"
  },
  "scripts": {
    "start": "set PORT=4000 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "build:dev": "npm run clean && set \"PUBLIC_URL=/grupo-01/dev\" && react-scripts build",
    "build:test": "npm run clean && set \"PUBLIC_URL=/grupo-01/test\" && react-scripts build",
    "build:staging": "npm run clean && set \"PUBLIC_URL=/grupo-01/staging\" && react-scripts build",
    "clean": "rimraf build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "cross-env": "^10.0.0"
  }
}
